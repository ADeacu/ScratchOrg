/// <reference types="node" />
import * as Stream from 'stream';
import Command from '../../../lib/base';
export default class LogTail extends Command {
    static summary: string;
    static examples: string[];
    static flags: {
        'target-compute': import("@oclif/core/lib/interfaces").OptionFlag<string | undefined>;
        environment: import("@oclif/core/lib/interfaces").OptionFlag<string | undefined>;
    };
    run(): Promise<void>;
    simpleStreamingStream(logSessionURL: string): Promise<Stream.Readable>;
    eventSourceStream(logSessionURL: string, tail: boolean): Stream.Readable;
    readLogs(logSessionURL: string, tail: boolean): Promise<void>;
}
